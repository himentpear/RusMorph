{
  "name": "werus-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "wrangler types && tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.7",
    "wrangler": "^4.120.0"
  }
}
